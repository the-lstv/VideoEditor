server {
    domains: video-editor.localhost;
}

# Copy for people who don't use Akeno or local (electron) builds
task-post-compile ("index.html") {
    copy: "index-generated.html";
}

browserSupport: {
    chrome: 134;
    firefox: 130;
}